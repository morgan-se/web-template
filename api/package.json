{
  "name": "sng301-towerdefense-enemies-api",
  "version": "0.0.1",
  "description": "API for retrieving enemies for SENG301 lab 6 and assignments (2025)",
  "main": "dist/server.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Morgan English",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "winston": "^3.8.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.14",
    "@types/node": "^18.11.9",
    "tslint": "^6.1.3",
    "typescript": "^4.8.4"
  }
}
